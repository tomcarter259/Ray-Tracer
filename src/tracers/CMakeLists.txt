CMAKE_MINIMUM_REQUIRED(VERSION 2.4)
PROJECT(tracers)

FILE(GLOB SRCS *.cpp)
FILE(GLOB HDRS *.hpp)

INCLUDE_DIRECTORIES(${SRC_DIR})
ADD_LIBRARY(tracers ${SRCS} ${HDRS})

TARGET_LINK_LIBRARIES(
	tracers
	common
)
