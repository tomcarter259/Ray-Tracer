CMAKE_MINIMUM_REQUIRED(VERSION 2.4)
PROJECT(lights)

FILE(GLOB SRCS *.cpp)
FILE(GLOB HDRS *.hpp)

INCLUDE_DIRECTORIES(${SRC_DIR})
ADD_LIBRARY(lights ${SRCS} ${HDRS})

TARGET_LINK_LIBRARIES(
	lights
	common
)
