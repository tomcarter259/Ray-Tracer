CMAKE_MINIMUM_REQUIRED(VERSION 2.4)
PROJECT(materials)

FILE(GLOB SRCS *.cpp)
FILE(GLOB HDRS *.hpp)

INCLUDE_DIRECTORIES(${SRC_DIR})
ADD_LIBRARY(materials ${SRCS} ${HDRS})

TARGET_LINK_LIBRARIES(
	materials
	common
)
